// M-00: test trivial que verifica la tubería. Lo sustituye M-11.
import { describe, expect, it } from 'vitest';

describe('andamiaje', () => {
  it('ejecuta la suite rápida', () => {
    expect(1 + 1).toBe(2);
  });
});
