# G-21 · Edificios de la aldea

16 sep 2026. Encargo: `encargo-blender-aldea.md`. Un commit y una subida por
modelo, antes de comenzar el siguiente. Esta ronda conserva ids y contratos
existentes, sin modificar `src/` ni los constructores.

## Casa de paja · house

Entramado de madera, basamento de piedra, contraventanas abiertas, puerta
enmarcada, aleros, capas de paja y chimenea cuadrada. Conserva parcela 2×2,
escala 1/3, orientación y las tres ventanas que usa la iluminación del juego.
Los marcos tienen nombres distintos de los huecos para no duplicar luces.

| | Antes | Ahora |
|---|---:|---:|
| Triángulos | 88 | 656 |
| Materiales / mallas | 5 / 5 | 5 / 5 |
| GLB | 10 720 bytes | 51 500 bytes |

El aumento es geometría de entramado y tejado; las llamadas de dibujo del
modelo no aumentan. No es una medición nueva de rendimiento en móvil.

Validación: typecheck, lint, 87 pruebas de efectos/mundo/estado escénico y
`npm run art -- all house`. Publicación selectiva de `house.glb` y su huella:
los demás recursos publicados se conservaron. El empaquetador aceptó los 50
recursos. Captura del juego en semilla 11, año 20, zoom 6, sin errores de página,
en `artifacts/graphics/G-21/delivery/house-in-game.png` (390×844 CSS, DPR 2).

Artefactos aprobados: `artifacts/graphics/G-21/approved/5fb010cd3d40d3e4/`.
Receta canónica: `art/recipes/house/house.json`. Evidencias y salida de las
pruebas/construcción en `artifacts/graphics/G-21/delivery/`.

Casa de paja subida en el commit `dcb4cf1` antes de comenzar la siguiente.

## Casa de piedra · stone-house

Mampostería con esquinas trabadas, dinteles gruesos, contraventanas y puerta
reforzada. La cumbrera gira 90 grados respecto a la casa de paja y las hiladas
de teja refuerzan la diferencia de silueta. Conserva la parcela 2×2, escala 1/3
y posiciones/dimensiones de sus tres huecos de luz.

| | Antes | Ahora |
|---|---:|---:|
| Triángulos | 88 | 752 |
| Materiales / mallas | 4 / 4 | 4 / 4 |
| GLB | — | 57 236 bytes |

Typecheck, lint y las mismas 87 pruebas pasan. Construcción y publicación
selectiva verificadas; los otros GLB se conservaron, incluida la casa de paja
ya subida. Captura del juego real: semilla 11, año 50, zoom 6 y giro −90°,
390×844 CSS, DPR 2, sin errores de página. En la captura aparecen ambas casas
junto al pozo y los aldeanos, permitiendo comparar su proporción y lectura.

Artefactos: `artifacts/graphics/G-21/approved/df75fd4689ed1ed6/`.
Captura: `artifacts/graphics/G-21/delivery/stone-house-in-game.png`.
Receta: `art/recipes/stone-house/stone-house.json`.

Quedan pendientes el resto del encargo y las variantes nuevas de vivienda.

## Molino · mill

Torre octogonal de piedra con basamento, cornisas, puerta enmarcada y cubierta
de paja. Cuatro aspas con bastidores de madera y paños claros independientes
del tejado. Conserva id, escala 1/3 y parcela 2×2; los límites horizontales
quedan dentro de ella y la altura es 2,657 celdas. Sigue estático: no incorpora
clips ni un mecanismo de giro en el juego.

Pasa de 90 a 966 triángulos y de tres a cuatro materiales/mallas. El material
adicional distingue los paños de la cubierta. GLB: 69 632 bytes. No se ha
medido de nuevo el rendimiento en móvil.

Construcción y validación de Blender aprobadas; typecheck, lint y 87 pruebas
de efectos/mundo/estado escénico pasan. Publicación selectiva verificada con
las huellas de los demás GLB intactas. Empaquetado de los 50 recursos correcto.
Captura real en semilla 11, año 50, zoom 3, sin errores de página: el molino
aparece junto a las viviendas, parcialmente tapado por ellas.

Receta: `art/recipes/mill/mill.json`.
Artefactos aprobados: `artifacts/graphics/G-21/approved/2aad24fd4802a4a1/`.
Captura y evidencias: `artifacts/graphics/G-21/delivery/mill-in-game.png`
y ficheros `mill-*.log`, `mill-verification.json`, `mill-before.json`.
Molino subido en `1889d40`.

## Herrería · smithy

Taller abierto de piedra con postes y viga de madera, cubierta de teja,
yunque sobre tocón, banco con martillo y barras, herramientas colgadas y
fragua posterior. El centro de la entrada queda libre. La chimenea conserva
su eje original (4,6; 4,5 en Blender) y prácticamente su altura: 4,2125 frente
a 4,2. Sin fuego permanente que contradiga el estado apagado del edificio.

Conserva id, escala 1/3, orientación y parcela 2×2: límites horizontales
X [0,001667; 1,998333], Z [−1,958333; −0,06]. Tiene 894 triángulos,
cuatro materiales/mallas y GLB de 67 080 bytes (antes: tres materiales,
GLB de 6 276 bytes). El material adicional corresponde a la madera;
el hierro usa el negro de la paleta compartida. No hay medida nueva en móvil.

Construcción, validación y publicación selectiva verificadas: los demás GLB
conservan sus huellas. Typecheck, lint y 87 pruebas pasan; el empaquetador
acepta los 50 recursos. Render de Blender revisado. **Revisión dentro del
juego pendiente:** el intento de captura fue rechazado por la revisión
automática al alcanzar el límite de uso; no se presenta como comprobada.

Receta: `art/recipes/smithy/smithy.json`.
Artefactos: `artifacts/graphics/G-21/approved/bb1cb2fc6312c38c/`.
Evidencias en `artifacts/graphics/G-21/delivery/`: `smithy-before.json`,
`smithy-verification.json` y registros `smithy-*.log`.
Antes de cerrar visualmente la herrería falta la captura real. Después sigue
el granero.

## Granero · granary

Almacén elevado sobre apoyos de piedra con guardas anchas, entablado,
portón doble, escalones, respiradero y cubierta de paja. Conserva id,
escala 1/3, orientación y parcela 2×2. Altura 1,765 celdas frente a 1,733;
límites horizontales X [0,015; 1,985], Z [−1,983333; 0].

Se retira la gavilla fija antigua: las existencias siguen mostrándose con el
montón variable de `effects/tells.ts`, fuera del edificio. No se modifica
ese código ni la elección del lado libre. El modelo no introduce sacos
permanentes que indiquen reservas cuando el almacén está vacío.

Pasa de 112 a 1 220 triángulos y de tres a cuatro materiales/mallas;
GLB de 89 972 bytes. El detalle de tablas y estructura eleva la geometría;
la piedra y el entramado sustituyen el material de grano estático. Sin una
nueva medición de rendimiento en móvil.

Blender y validación GLB aprobados, typecheck, lint y 87 pruebas verdes.
Publicación selectiva con huellas ajenas conservadas; empaquetados 50 recursos.
Render de Blender y captura real revisados: semilla 11, año 50, zoom 1,
giro 90°, 390×844 CSS, DPR 2. Granero visible junto al campo y al montón
variable de grano; sin errores de página. La captura volvió a estar disponible
en esta entrega. La vista de la herrería es posterior y no cierra su revisión
frontal pendiente.

Receta: `art/recipes/granary/granary.json`.
Artefactos: `artifacts/graphics/G-21/approved/0d35c7e1b2a0801a/`.
Evidencias: `artifacts/graphics/G-21/delivery/granary-in-game.png`,
`granary-before.json`, `granary-verification.json` y `granary-*.log`.
Siguiente modelo: capilla.

## Capilla · chapel

Nave estrecha con contrafuertes, portada de piedra, óculo, huecos laterales
y espadaña abierta con campana y cruz. Conserva la nave y la puerta originales,
id, escala 1/3, orientación, parcela 2×2 y altura máxima de 2,1 celdas.
Las velas siguen siendo un efecto dinámico situado por el juego; no se añaden
luces permanentes ni se modifica su colocación. Campana estática, sin clip nuevo.

Pasa de 56 a 776 triángulos y de tres a cuatro materiales/mallas. El material
oscuro permite leer los huecos y herrajes. GLB de 57 672 bytes. No se ha vuelto
a medir rendimiento en móvil.

Blender/GLB validados, typecheck, lint y 87 pruebas verdes; publicación
selectiva con los demás recursos intactos y empaquetado de 50 recursos correcto.
Tras el primer render se baja y reduce el óculo para que no lo tape el tejado;
la construcción final vuelve a pasar la validación. Captura real revisada:
semilla 2, año 60, zoom 1, 390×844 CSS, DPR 2, sin errores de página. La capilla
aparece completa junto al bosque y permite comparar su escala con las casas.
Esta misma captura muestra **la fachada abierta de la herrería**, y cierra
su revisión en juego que había quedado pendiente por el límite de uso.

Receta: `art/recipes/chapel/chapel.json`.
Artefactos: `artifacts/graphics/G-21/approved/ce20d06c0e4e32fc/`.
Evidencias: `artifacts/graphics/G-21/delivery/chapel-in-game.png`,
`chapel-before.json`, `chapel-verification.json` y `chapel-*.log`.
Siguiente modelo: iglesia.

## Iglesia · church

Nave de piedra con contrafuertes y ventanas estrechas, torre cuadrada con
campanario abierto, campana, chapitel y cruz. Portada enmarcada y tejado
escalonado para que no atraviese el vano de la campana. Se corrigen dos
defectos del recurso anterior: puerta enterrada en la torre y geometría que
rebasaba la huella declarada de 2×2. La altura sigue siendo 3,716667 celdas;
límites horizontales finales X [0,078333; 1,921667], Z [−2; 0].

**Distinción de integración:** la receta declara 2×2, como antes, pero el
motor asigna a `church` una parcela de 3×3. No se cambia esa regla, ni el
origen, escala 1/3, orientación o selección del recurso. Las velas siguen
siendo dinámicas; la campana es estática y no añade clips.

Pasa de 74 a 1 230 triángulos y de tres a cuatro materiales/mallas, con
89 412 bytes de GLB. El material oscuro distingue vanos y herrajes. Sin una
nueva medición de rendimiento en móvil.

Blender y GLB finales validados; typecheck, lint y 87 pruebas verdes.
Publicación selectiva con huellas ajenas conservadas, 50 recursos empaquetados.
Render final revisado tras separar el tejado del campanario. Captura real:
semilla 43, año 60, zoom 0, con lluvia, 390×844 CSS, DPR 2, sin errores de página.
Se centra la iglesia con arrastre de cámara (80,430) → (290,400) CSS; copia
del capturador con ese gesto en `delivery/church-shot.mjs`. La iglesia aparece
completa y se compara con las casas y los aldeanos.

Receta: `art/recipes/church/church.json`.
Artefactos: `artifacts/graphics/G-21/approved/92b8dbffc1221461/`.
Evidencias: `artifacts/graphics/G-21/delivery/church-in-game.png`,
`church-before.json`, `church-verification.json`, `church-shot.mjs` y
`church-*.log`. Siguiente modelo: pozo.

## Pozo · well

Brocal hueco de mampostería con tres hiladas y albardilla, postes de madera,
cubierta de paja, torno con manivela y cuerda, y cubo lateral. Sustituye el
cilindro macizo original por paredes separadas y fondo oscuro. Todos los
elementos son estáticos, sin cambios en la interacción del juego.

Conserva id, escala 1/3, parcela 1×1 y altura máxima de 1,033333 celdas.
Límites horizontales X [0,128333; 0,952667], Z [−0,9; −0,1]. Pasa de 84 a
1 108 triángulos y de tres a cuatro materiales/mallas; GLB de 78 572 bytes.
La geometría adicional forma las hiladas, el torno y el cubo; el material
oscuro separa hueco, aros y herrajes. Sin medición nueva de rendimiento móvil.

Blender y GLB aprobados; typecheck, lint y 87 pruebas verdes. Publicación
selectiva con los demás recursos intactos y empaquetado de 50 recursos.
Render de Blender y captura cercana real revisados: semilla 11, año 50,
zoom 6 y giro −90°, 390×844 CSS, DPR 2, sin errores de página. Pozo entre
viviendas, con una parte tapada por la casa vecina; el brocal hueco y el torno
son visibles. Vista general adicional con zoom 2 y el mismo giro.

Receta: `art/recipes/well/well.json`.
Artefactos: `artifacts/graphics/G-21/approved/a07afdc2d123322b/`.
Evidencias: `artifacts/graphics/G-21/delivery/well-in-game.png`,
`well-in-game-wide.png`, `well-before.json`, `well-verification.json` y
`well-*.log`. Con esto quedan modelados los seis edificios de la tanda 2.
Siguiente recurso: campo cultivado (`field`).

## Campo cultivado · field

Cinco surcos de tierra, cada uno con doble hilera de tallos y espigas facetadas.
Sustituye las cinco franjas macizas antiguas; cabeceras bajas y sin adornos.
Conserva id, escala 1/3, orientación y parcela exacta 3×2. La altura pasa de
0,166667 a 0,281667 celdas para que se lea como cultivo en pie, por debajo de
la cintura de los aldeanos. El primer render quedó demasiado ralo y bajo;
la versión final duplica las hileras y eleva las espigas.

Dos materiales/mallas, como antes; pasa de 72 a 2 056 triángulos y el GLB
ocupa 160 704 bytes. El coste adicional permite separar visualmente las
plantas; no aumentan las llamadas por material. Al repetirse en muchas
parcelas, el aumento de geometría se acumula; no se ha medido FPS en móvil.

La selección estacional se mantiene: `field` desde la semana 6 hasta antes
de la 35, y `field-cut` el resto, según `world/plan.ts`. No se introduce
crecimiento gradual, animación ni selección por rendimiento del cultivo.
El campo segado se rediseña en la siguiente entrega, por separado.

Construcción y GLB finales aprobados, typecheck, lint y 87 pruebas verdes.
Publicación selectiva con huellas ajenas intactas y 50 recursos empaquetados.
Render y captura real revisados: semilla 11, año 50, avance de 12 semanas
hasta verano, zoom 2, 390×844 CSS, DPR 2, sin errores de página. Se ven
parcelas cultivadas junto a aldeanos trabajando y parcelas sin cultivo.

Receta: `art/recipes/field/field.json`.
Artefactos: `artifacts/graphics/G-21/approved/989feea131186480/`.
Evidencias: `artifacts/graphics/G-21/delivery/field-in-game.png`,
`field-before.json`, `field-verification.json` y `field-*.log`.
Siguiente recurso: campo segado (`field-cut`).

## Carro · handcart

Caja vacía con cinco tablas de suelo, tres tablas por lateral y testero,
postes de esquina y remaches. Dos ruedas abiertas con seis radios, bujes de
madera y aros oscuros sustituyen los discos macizos. Conserva el eje y los
dos varales originales, la escala 1/3 y el origen centrado del adorno.
Es estático; mantiene la colocación determinista existente, sin cambios del motor.

Pasa de 580 a 888 triángulos y de dos a tres materiales/mallas. GLB de
64 556 bytes. Altura máxima de 0,3 celdas, frente a 0,27; tamaño horizontal
0,696646 × 0,350667. Las coordenadas negativas son propias del origen
centrado. Altura mínima 0,000107: ruedas y varales no atraviesan el suelo.
El primer aro se corrigió antes de aprobar esta entrega.

Blender y GLB aprobados; typecheck, lint y 94 pruebas verdes en cuatro
ficheros, incluidas las seis de colocación de adornos. Publicación selectiva
con huellas ajenas intactas y empaquetado de 52 recursos. Render de Blender
y dos capturas reales revisados, sin errores de página: semilla 11, año 50,
zoom 1 y giro 90°; semilla 2, año 50, zoom 3. En la segunda se distingue
la caja junto a las casas y los aldeanos; los troncos ocultan parte de las
ruedas. La geometría completa se verifica en el render aislado.
No se ha realizado una nueva medición de FPS en móvil.

Receta: `art/recipes/handcart/handcart.json`.
Artefactos: `artifacts/graphics/G-21/approved/3ad96d3b5da5c2ef/`.
Evidencias: `artifacts/graphics/G-21/delivery/handcart-in-game.png`,
`handcart-in-game-close.png`, `handcart-before.json`,
`handcart-verification.json` y `handcart-*.log`.
Siguiente recurso: almiar (`haystack`). Avisar al dueño antes de los muros.

## Almiar · haystack

Tres faldones de heno superpuestos, haces inclinados, mechones en la base y
estaca central. Sustituye el volumen liso original. Conserva id, escala 1/3,
origen centrado, parcela 1×1 y altura de la estaca de 0,666667 celdas.
Es estático y mantiene la colocación existente junto a los campos.

Pasa de 68 a 958 triángulos y de dos a tres materiales/mallas. La textura
del heno es geometría, sin imágenes externas. No hay medición nueva de FPS.
Se corrigieron un escalón en la base y un desplazamiento de los haces que
solo aparecía al instanciar en el juego. Las mallas se unen con un pequeño
elemento de anclaje en el origen por material, conservando sus orígenes
comunes sin modificar el render ni los demás recursos.

Construcción y GLB aprobados. Typecheck, lint y 94 pruebas verdes; publicación
selectiva con huellas ajenas intactas. Bundle de 52 recursos. Render aislado
y captura real revisados: semilla 2, año 50, zoom 3, 390×844 CSS, DPR 2,
sin errores de página. Se ven dos almiares completos junto a casas y aldeanos,
con los haces en su posición y las estacas visibles.

Receta: `art/recipes/haystack/haystack.json`.
Artefactos finales: `artifacts/graphics/G-21/approved/be3a923e28bdc16b/`.
Evidencias: `artifacts/graphics/G-21/delivery/haystack-in-game.png`,
`haystack-before.json`, `haystack-verification.json` y `haystack-*.log`.
Siguiente recurso: pila de leña (`log-pile`). Avisar antes de los muros.

## Pila de leña · log-pile

Cinco troncos en dos hileras, con corteza facetada y relieves longitudinales,
cortes claros en ambos extremos, anillos de duramen y grietas. Longitudes
ligeramente distintas para que los extremos no formen una pared uniforme.
Mantiene id, origen centrado, eje X longitudinal, escala 1/3 y parcela 1×1.
Mallas con origen común para su colocación por instancias, como el almiar.
Adorno estático, sin cambios en reservas, simulación ni selección de lugares.

Pasa de 196 a 1 544 triángulos; conserva dos materiales/mallas. GLB de
97 652 bytes; tamaño 0,53 × 0,34 celdas y altura máxima 0,2072.
No se ha medido de nuevo el rendimiento móvil.

Construcción y GLB aprobados; typecheck, lint y 94 pruebas verdes, incluidas
las de colocación de adornos. Publicación selectiva con huellas de los otros
recursos intactas. Bundle de 52 recursos. Render de Blender y captura real
revisados: semilla 2, año 50, zoom 3, 390×844 CSS, DPR 2, sin errores de
página. Se ven varias pilas junto a las casas, con los cortes alineados y
escala comparable a los aldeanos.

Receta: `art/recipes/log-pile/log-pile.json`.
Artefactos: `artifacts/graphics/G-21/approved/3ec2dfbe987f7f2e/`.
Evidencias: `artifacts/graphics/G-21/delivery/log-pile-in-game.png`,
`log-pile-before.json`, `log-pile-verification.json` y `log-pile-*.log`.
Siguiente recurso: cobertizo (`shed`). Avisar antes de los muros.

### Ajuste de tamaño de la leña

El dueño pide hacerla un poco más pequeña. Geometría reducida un 10 % en
los tres ejes, conservando forma, origen, materiales y escala de exportación
1/3. Cambiar solo la escala del nodo no se reflejaba en las instancias del juego.
Nuevo tamaño horizontal 0,477 × 0,306 celdas; altura máxima 0,18648.
Reconstrucción y validación del GLB correctas, publicación selectiva y captura
real revisada con la misma semilla 2, año 50 y zoom 3, sin errores de página.
No se repiten las pruebas de código: solo cambia la escala de la receta.
Artefactos vigentes: `artifacts/graphics/G-21/approved/48a29eb876083634/`.
Captura: `artifacts/graphics/G-21/delivery/log-pile-smaller-in-game.png`.

## Cobertizo · shed

Almacén abierto con tres paredes de tablas, cuatro postes, travesaños,
refuerzos de entrada, estante interior y tejado de paja con listones y remates.
Conserva id, escala 1/3 y parcela 1×1. Altura máxima 0,912712 celdas;
los pequeños remates superan la altura original de 0,9. Sin clips.
844 triángulos, tres materiales/mallas y GLB de 62 564 bytes.

Blender y GLB aprobados, renders de ambos revisados. Publicación selectiva
con huellas ajenas intactas; bundle de 52 recursos. Typecheck y 94 pruebas
verdes. Lint global falla en un archivo ajeno aparecido durante la ronda:
`compare.mjs:55`, variable `row` sin usar; no se modifica ese trabajo.

**Integración pendiente:** el juego no selecciona `shed`; esto ya ocurría
con el recurso original. Se entrega el modelo de catálogo, sin inventar una
función económica ni añadir edificios al motor. Las capturas aprobadas son
del visor aislado, no acreditan presencia dentro de una partida.

Receta: `art/recipes/shed/shed.json`.
Artefactos: `artifacts/graphics/G-21/approved/c5ccd1eab317043b/`.
Evidencias: `artifacts/graphics/G-21/delivery/shed-before.json`,
`shed-verification.json` y `shed-*.log`.
La siguiente tanda es defensas (`palisade`, `wall`): avisar al dueño antes
de comenzarla, como pidió. No se han modelado todavía.
