# Informe G-03 · Primer estudio de dirección artística

**Spec de partida:** v2.92 · **Ejecución:** 9 sep 2026, 20:29 CEST
**Informe redactado:** 13 sep 2026, por el agente del motor, a partir de los
artefactos que la ronda dejó. Ver «Nota sobre esta redacción» al final.
**Estado:** dos direcciones producidas y validadas. **La elección es del
usuario y no se ha hecho.**

## Resultado

Existen dos rincones de aldea completos y comparables, `village-corner-a` y
`village-corner-b`, construidos con la misma cadena de G-02 —receta declarativa
→ Blender → GLB → Three.js → promoción— sin abrir Blender a mano.

Los dos tienen exactamente el mismo contenido, la misma huella, la misma
orientación, la misma cámara y la misma resolución. **Lo único que cambia entre
ellos son decisiones artísticas declaradas** en `art/recipes/palette.json`, que
es lo que la ronda pedía para que la comparación signifique algo.

| | Dirección A · «cuento tallado» | Dirección B · «acuarela seca» |
|---|---|---|
| Tejado | Teja, `#7A382B` | Paja, `#C7984A` |
| Yeso | `#D2B98A` | `#E2CC9B` |
| Entramado | `#3F2B22`, marcado | `#6B4C32`, suave |
| Pradera | `#73834F` | `#94AE68` |
| Lectura | Contraste alto, sombras profundas | Claro y desaturado |

Las capturas están en `artifacts/graphics/G-03/approved/`, cada una con su
versión en escala de grises para juzgar la legibilidad sin color:

- A: `97cd165b1713778f/capture-1.png`
- B: `445d8579e62204d8/capture-1.png`

## Qué está verificado

Las dos direcciones pasan la validación completa de G-02, con `status: pass`:

| Comprobación | A | B |
|---|---|---|
| Contenedor GLB 2 y chunk JSON | ✅ | ✅ |
| Límites POSITION finitos en tres ejes | ✅ | ✅ |
| Nombres de objeto y material únicos y esperados | ✅ | ✅ |
| Carga real en Three.js, no simulada | ✅ | ✅ |
| Captura repetible en el mismo equipo | ✅ | ✅ |
| Límites de mundo finitos y positivos | ✅ | ✅ |
| Errores de navegador | ninguno | ninguno |

**La repetición es real, no declarada:** las dos capturas de cada dirección
salieron con el mismo SHA-256, y el manifiesto lo marca como
`deterministicOnThisHost`. La cadena también comprueba que un recurso ausente
falla de forma legible en vez de dejar una pantalla en blanco.

Coste geométrico de la dirección A, medido: 45 objetos, 41 mallas, 13 materiales
y **2.262 triángulos** para una parcela con casa, campo con surcos, camino, tres
árboles y un aldeano de pie.

Entorno: Node.js 24.15.0, Three.js 0.185.0, Chrome 153.0.8010.36, vista de
390×640 con cámara `iso-ne`, que es el móvil que §11 toma como referencia.

## Qué NO está verificado

- **El aspecto no está aceptado.** D.0 dice que la aceptación visual es del
  usuario, y no ha ocurrido. Este informe presenta, no aprueba.
- **Nada de esto toca el juego.** Los recursos están en estado `study`. El valle
  que se juega sigue siendo el Canvas de §10 y §11; `src/render3d/contracts.ts`
  no lo importa nadie todavía.
- **Sólo se ha probado en este equipo.** La repetición entre máquinas distintas
  sigue pendiente desde G-02 y no bloquea la producción local.
- **No hay rig ni animación.** El aldeano tiene jerarquía de miembros
  (`Villager_UpperArm_L`, `Villager_Shin_R`…) pero `animationNames` está vacío.
  Eso es G-04 y la ronda tenía prohibido empezarlo.

## Lo que hay que decidir

**Cuál de las dos direcciones, o ninguna.** Es la pregunta que la ronda existía
para poder hacer, y ahora se puede hacer mirando dos imágenes en vez de
discutiendo adjetivos.

Un apunte que las capturas permiten y conviene decir: en escala de grises la
dirección A separa mejor el tejado de la pared, y B separa mejor la casa del
prado. Si la legibilidad a tamaño de móvil pesa más que el ambiente, eso es un
dato; si pesa el ambiente, no lo es.

## Nota sobre esta redacción

Este informe lo escribe el agente del motor, no el que ejecutó la ronda. La
ronda produjo las recetas, los modelos, las capturas y los manifiestos, y se
detuvo antes de escribirlo.

**Todo lo que dice sale de los artefactos**: los manifiestos `validation.json` y
`capture.json`, la paleta y las dos imágenes. No se ha vuelto a ejecutar la
cadena ni se ha tocado una receta, y ninguna afirmación de aquí procede de
suponer qué pretendía el autor. Donde no había evidencia —la elección de
dirección— se dice que no la hay.
